/* hello.c 
 * Programme d'exemple permettant d'afficher la phrase
 * "Hello World" sur le terminal.
 *
 * Compilez ce programme avec la commande :
 * $> gcc -W -Wall -pedantic hello.c -o hello
 *
 * Exécutez avec la commande :
 * $> ./hello
 */
#include <stdio.h> 

int main() 
{ 
    printf("Hello World!\n") ; 

    return 0 ; 
} 